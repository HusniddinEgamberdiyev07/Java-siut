class Test{
    int a;

    void display(){
        System.out.println(a);
    }
}


public class Access {
    
}

// public private default protected
// access specifiers tells which part of your can be accessed

// default -> can be used in the same class. same package sub-class. same package not sub-class
// private -> only in the same class
// protected -> same package, same class, same package sub-class, different package sub-class
// public -> anywhere
